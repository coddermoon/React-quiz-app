import React from 'react';
import './Options.css'

const Options = ({option}) => {
    return (
        <div className='element'>
          
            {option}
        </div>
    );
};

export default Options;